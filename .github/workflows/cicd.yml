name: Deploy

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  Deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [ '1.22' ]

    steps:
      - uses: actions/checkout@v2
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go-version }}
      - name: Install Mockery
        run: |
          go install github.com/vektra/mockery/v2.43.1
          export PATH=$PATH:$(go env GOPATH)/bin
      - name: Build & Deploy
        run: |
          go version
          go mod tidy
