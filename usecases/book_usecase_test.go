package usecases_test

import (
	"testing"
)

func TestAddNewBook(t *testing.T) {
	t.Run("success to create add book", func(t *testing.T) {
		//request := &requests.CreateBookRequest{}
		//ctx := context.WithValue(context.TODO(), enums.UserID, 1)

		//usecases := mocks.NewBookUsecase()
		//usecases.On("AddNewBook", ctx, request, nil).Return(mock.AnythingOfType("*responses.CreateBookResponse"), nil).Once()
	})

	t.Run("", func(t *testing.T) {

	})
}
